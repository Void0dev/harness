interface:
  display_name: "Install Project Harness"
  short_description: "Deploy one repository's customer chat in its Coolify production environment"
  default_prompt: "Use $deploy-opencode-harness to install Harness for one existing GitHub repository in its existing Coolify environment. Show the short prerequisite checklist, then collect all six project fields in one message. Never ask me to paste a PEM or token."
