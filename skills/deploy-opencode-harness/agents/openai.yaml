interface:
  display_name: "Deploy OpenCode Harness"
  short_description: "Install or resume one isolated repository Harness"
  default_prompt: "Use $deploy-opencode-harness to discover available server access, guide the repository owner through the Release App checkpoint, install one public Harness plus one private OpenCode Runtime, verify it, and return exactly url, login, and password."
